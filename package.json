{
    "name": "team-scheduler-backend",
    "version": "1.0.0",
    "description": "Backend server for the Team Scheduler application.",
    "main": "server.js",
    "scripts": {
      "start": "node server.js",
      "dev": "nodemon server.js",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
      "express": "^4.18.2",         // Web framework (Version might vary slightly after install)
      "express-session": "^1.17.3", // Session management (Version might vary)
      "mysql2": "^3.6.5"            // MySQL driver (Version might vary)
    },
    "devDependencies": {
      "nodemon": "^3.0.2"          // Utility to auto-restart server on file changes (Version might vary)
    },
    "author": "Your Name",          // Optional: Put your name here
    "license": "ISC"              // Or MIT, UNLICENSED, etc.
  }
